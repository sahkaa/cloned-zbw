apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: cloned-zbw-dev-ingress
  annotations:
    kubernetes.io/ingress.class: nonprod-internal
    nginx.ingress.kubernetes.io/rewrite-target: /
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
spec:
  rules:
  - host:
    http:
      paths:
      - path: /cloned-zbw-api-dev
        backend:
          serviceName: cloned-zbw-api-service
          servicePort: 8080
      - path: /cloned-zbw-ui-dev
        backend:
          serviceName: cloned-zbw-ui-service
          servicePort: 80

